'use client';

import React from 'react';

interface FirstWorkspaceModalProps {
  isOpen: boolean;
}

/**
 * Legacy wrapper: Centralized first workspace modal is automatically orchestrated by WorkspaceModalProvider in DashboardShell
 */
export const FirstWorkspaceModal: React.FC<FirstWorkspaceModalProps> = () => {
  return null;
};
