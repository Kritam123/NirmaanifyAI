import React from 'react';
import { GlobalLoadingScreen } from '@nirmaanify/ui';

export default function DashboardLoading() {
  return <GlobalLoadingScreen message="Architecting workspace environment..." />;
}
